import { StyleSheet } from 'react-native';
import { COINTIKO_HEADER_COLOR } from '../values';


export const appHeaderStyle = StyleSheet.create({
    container: {
        backgroundColor: COINTIKO_HEADER_COLOR
    }
})